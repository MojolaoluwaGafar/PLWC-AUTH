TOKEN
used to share information between two entities, usually a client (like your app's frontend) and a server (your app's backend).
JSONWEBTOKEN
They contain JSON objects which have the information that needs to be shared.